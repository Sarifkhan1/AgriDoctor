# AgriDoctor AI - Runtime + dev dependencies (serving API)
# Heavy offline ML/training deps live in requirements-ml.txt (used only by src/).

# --- Web framework ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.2.1

# --- Security & configuration ---
python-jose[cryptography]>=3.3.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.1.0

# --- AI provider (open-source models on Groq) ---
groq>=0.11.0

# --- Image handling / upload validation ---
Pillow>=10.1.0

# --- HTTP ---
httpx>=0.25.0
requests>=2.31.0

# --- Local CNN inference (optional — only needed if use_local_cnn=True) ---
torch>=2.1.0
torchvision>=0.16.0

# --- Dev / test ---
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.11.0
isort>=5.12.0
