# AgriDoctor AI - ML dependencies.
#
# Needed to TRAIN (scripts/prepare_dataset.py, train_cnn.py, evaluate_cnn.py)
# and to SERVE the local CNN, which is the primary diagnosis engine — torch and
# torchvision are required at runtime whenever USE_LOCAL_CNN=true.
#
# To run the API in cloud-only mode (USE_LOCAL_CNN=false), requirements.txt
# alone is enough; the CNN imports are lazy so the app starts without torch.

-r requirements.txt

# ML core
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.35.0

# Vision / audio processing
opencv-python-headless>=4.8.0
openai-whisper>=20231117
librosa>=0.10.1
soundfile>=0.12.1

# Data / science
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Plots: training curves + confusion matrix (scripts/train_cnn.py, evaluate_cnn.py)
matplotlib>=3.8.0

# Config / utils
PyYAML>=6.0.1
tqdm>=4.66.0

# Annotation tool
streamlit>=1.28.0

# Data download
kaggle>=1.6.0
datasets>=2.14.0  # HuggingFace datasets — used by scripts/train_real.py + train_fusion_real.py
